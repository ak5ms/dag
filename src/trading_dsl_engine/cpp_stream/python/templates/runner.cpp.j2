// Generated by trading_dsl_engine.cpp_stream/python/templates/runner.cpp.j2.
#include <array>
#include <atomic>
#include <chrono>
#include <cstddef>
#include <cstdint>
#include <ctime>
#include <exception>
#include <memory>
#include <string>
#include <thread>
#include <vector>

#include "stackdsl/runtime.hpp"
{% for header in native_headers %}
#include "{{ header }}"
{% endfor %}

namespace {
thread_local std::string g_last_error;

using PublicOutputs = stackdsl::OutputLayout<
{% for output in outputs %}
    stackdsl::OutputSpec<
        {{ output.offset }},
        {{ output.size }},
        {{ "true" if output.mode == "final" else "false" }},
        {{ "true" if output.lane_partitionable else "false" }}
    >{% if not loop.last %},{% endif %}
{% endfor %}
>;

{# Lowering compacts scalar scratch independently per native dtype and carries the
   exact counts on the int-compatible scratch_slots plan value. #}
{% set scalar_counts = scratch_slots.counts %}
using ScalarScratch = stackdsl::ScalarScratchLayout<
    {{ scalar_counts[0] }}, {{ scalar_counts[1] }}, {{ scalar_counts[2] }},
    {{ scalar_counts[3] }}, {{ scalar_counts[4] }}, {{ scalar_counts[5] }}
>;

{% for inner in inners %}
template <std::size_t N, std::size_t Capacity, std::size_t PartitionCount>
struct {{ inner.name }} {
    stackdsl::GroupRowContext<
        N,
        {{ inner.input_count }},
        {{ inner.scratch_slots }},
        {{ inner.matrix_scratch_slots }},
        {{ inner.matrix_scratch_width }},
        {{ inner.matrix_scratch_layout }}
    > ctx{};
{% for stage in inner.stages %}
    {{ stage.cpp_type }} s{{ stage.index }}{};
{% endfor %}

    void setup() noexcept {
{% for stage in inner.stages %}
        s{{ stage.index }}.setup();
{% endfor %}
    }

    void on_data(
        const std::array<const double*, {{ inner.input_count }}>& feeds,
        const std::array<std::uint16_t, N>& slots,
        const std::array<std::uint16_t, N>& partitions,
        double* out,
        std::size_t lane_begin,
        std::size_t lane_end) noexcept {
        ctx.inputs = feeds;
        ctx.group_slots = &slots;
        ctx.partitions = &partitions;
        ctx.output = out;
        ctx.lane_begin = lane_begin;
        ctx.lane_end = lane_end;
{% for stage in inner.stages %}
        s{{ stage.index }}.on_data(ctx);
{% endfor %}
    }
};
{% endfor %}

struct CppStreamPlanRunner {
    static constexpr std::size_t N = {{ n }};
    static constexpr std::size_t row_output_width = PublicOutputs::row_width;
    static constexpr std::size_t final_output_width = PublicOutputs::final_width;

    stackdsl::RowContext<
        N,
        {{ input_count }},
        {{ scratch_slots }},
        {{ matrix_scratch_slots }},
        {{ matrix_scratch_width }},
        {{ matrix_scratch_layout }},
        ScalarScratch
    > ctx{};
{% for stage in stages %}
    {{ stage.cpp_type }} s{{ stage.index }}{};
{% endfor %}

    void setup() noexcept {
{% for stage in stages %}
        s{{ stage.index }}.setup();
{% endfor %}
    }

    int run(
        const std::array<const void*, {{ input_count }}>& input_data,
        std::size_t row_begin,
        std::size_t row_end,
        double* STACKDSL_RESTRICT row_out,
        std::size_t lane_begin,
        std::size_t lane_end) noexcept {
        ctx.lane_begin = lane_begin;
        ctx.lane_end = lane_end;
        for (std::size_t t = row_begin; t < row_end; ++t) {
{% if prefetch_rows > 0 %}
{% for input in inputs %}
            if (t + {{ prefetch_rows }} < row_end) {
                __builtin_prefetch(
                    static_cast<const {{ input.cpp_type }}*>(input_data[{{ input.index }}])
                        + (t + {{ prefetch_rows }}) * {{ input.row_width }},
                    0,
                    1);
            }
{% endfor %}
{% endif %}
{% for input in inputs %}
            ctx.inputs[{{ input.index }}] =
                static_cast<const {{ input.cpp_type }}*>(input_data[{{ input.index }}])
                    + t * {{ input.row_width }};
{% endfor %}
            if constexpr (row_output_width > 0) {
                ctx.row_output = row_out + t * row_output_width;
                ctx.output = ctx.row_output;
            } else {
                ctx.row_output = nullptr;
                ctx.output = nullptr;
            }
{% for stage in stages %}
{% if not stage.final_on_data %}
{% if stage.checked %}
            if (!s{{ stage.index }}.on_data_checked(ctx)) return {{ stage.error_code }};
{% else %}
            s{{ stage.index }}.on_data(ctx);
{% endif %}
{% endif %}
{% endfor %}
        }
        return 0;
    }

{% if has_final_output %}
    int finalize(double* STACKDSL_RESTRICT final_out) noexcept {
        // Keep ctx.row_output on the last processed row so final-only descendants
        // may consume a public row subgraph without re-materializing it.
        ctx.output = final_out;
{% for stage in stages %}
{% if stage.final_on_data %}
{% if stage.checked %}
        if (!s{{ stage.index }}.on_data_checked(ctx)) return {{ stage.error_code }};
{% else %}
        s{{ stage.index }}.on_data(ctx);
{% endif %}
{% endif %}
{% if stage.finalizer %}
        s{{ stage.index }}.finalize(ctx);
{% endif %}
{% endfor %}
        return 0;
    }
{% endif %}
};

int run_bound_inputs(
    const std::array<const void*, {{ input_count }}>& input_data,
    std::size_t rows,
    const char* output_path,
    std::size_t output_offset_bytes,
    std::size_t async_writeback_bytes,
    std::size_t requested_threads,
    int parallel_mode,
    bool pin_threads,
    std::size_t* rows_out,
    double* seconds_out,
    double* cpu_seconds_out,
    std::size_t* threads_out,
    std::size_t* available_cpus_out) {
    constexpr std::size_t N = {{ n }};
    constexpr std::size_t row_output_width = PublicOutputs::row_width;
    constexpr std::size_t final_output_width = PublicOutputs::final_width;
    const std::size_t row_output_values = rows * row_output_width;
    const std::size_t output_payload_values = row_output_values + final_output_width;
    const std::size_t output_payload_bytes = output_payload_values * sizeof(double);
    if (output_offset_bytes % alignof(double) != 0) return 6;
    stackdsl::MMapFile output(
        output_path,
        true,
        output_offset_bytes + output_payload_bytes
    );
    output.advise_sequential();
    auto* STACKDSL_RESTRICT out = reinterpret_cast<double*>(
        static_cast<std::byte*>(output.data()) + output_offset_bytes
    );
    double* STACKDSL_RESTRICT row_out = out;
    double* STACKDSL_RESTRICT final_out = out + row_output_values;

    const std::size_t available_cpus = stackdsl::available_cpu_count();
    if (available_cpus_out) *available_cpus_out = available_cpus;

    std::size_t threads = 1;
{% if not has_final_output %}
    if (parallel_mode == 1) {
        threads = stackdsl::resolved_thread_count(requested_threads, rows);
    } else if (parallel_mode == 2) {
        if constexpr (!PublicOutputs::row_lane_partitionable) {
            return 5;
        }
        threads = stackdsl::resolved_thread_count(requested_threads, N);
    }
{% endif %}
    if (threads_out) *threads_out = threads;

    std::vector<std::unique_ptr<CppStreamPlanRunner>> runners;
    runners.reserve(threads);
    for (std::size_t worker = 0; worker < threads; ++worker) {
        auto runner = std::make_unique<CppStreamPlanRunner>();
        runner->setup();
        runners.push_back(std::move(runner));
    }

    std::atomic<int> error{0};
    const std::clock_t cpu_started = std::clock();
    const auto started = std::chrono::steady_clock::now();

    if (threads == 1) {
        int code = runners[0]->run(input_data, 0, rows, row_out, 0, N);
{% if has_final_output %}
        if (code == 0) code = runners[0]->finalize(final_out);
{% endif %}
        if (code != 0) error.store(code, std::memory_order_relaxed);
    } else {
        std::vector<std::thread> workers;
        workers.reserve(threads);
        for (std::size_t worker = 0; worker < threads; ++worker) {
            workers.emplace_back([&, worker] {
                if (pin_threads) stackdsl::pin_current_thread(worker);
                int code = 0;
                if (parallel_mode == 1) {
                    const std::size_t begin = rows * worker / threads;
                    const std::size_t end = rows * (worker + 1) / threads;
                    code = runners[worker]->run(
                        input_data, begin, end, row_out, 0, N
                    );
                } else {
                    const std::size_t begin = N * worker / threads;
                    const std::size_t end = N * (worker + 1) / threads;
                    code = runners[worker]->run(
                        input_data, 0, rows, row_out, begin, end
                    );
                }
                if (code != 0) {
                    int expected = 0;
                    error.compare_exchange_strong(
                        expected, code, std::memory_order_relaxed);
                }
            });
        }
        for (auto& worker : workers) worker.join();
    }

    const auto ended = std::chrono::steady_clock::now();
    const std::clock_t cpu_ended = std::clock();
    const int code = error.load(std::memory_order_relaxed);
    if (code != 0) return code;

    if (async_writeback_bytes > 0 && output_payload_bytes > 0) {
        output.request_writeback(output_offset_bytes, output_payload_bytes);
    }
    if (rows_out) *rows_out = rows;
    if (seconds_out) {
        *seconds_out = std::chrono::duration<double>(ended - started).count();
    }
    if (cpu_seconds_out) {
        *cpu_seconds_out = static_cast<double>(cpu_ended - cpu_started)
            / static_cast<double>(CLOCKS_PER_SEC);
    }
    return 0;
}
}  // namespace

extern "C" const char* cpp_stream_last_error() noexcept {
    return g_last_error.c_str();
}

extern "C" int cpp_stream_run_arrays(
    const void* const* input_data,
    const std::size_t* input_rows,
    const std::size_t* input_widths,
    std::size_t input_count,
    const char* output_path,
    std::size_t output_offset_bytes,
    std::size_t async_writeback_bytes,
    std::size_t requested_threads,
    int parallel_mode,
    bool pin_threads,
    std::size_t* rows_out,
    double* seconds_out,
    double* cpu_seconds_out,
    std::size_t* threads_out,
    std::size_t* available_cpus_out) noexcept {
    try {
        g_last_error.clear();
        if (
            input_count != {{ input_count }} || input_data == nullptr ||
            input_rows == nullptr || input_widths == nullptr || output_path == nullptr
        ) return 1;
        const std::size_t rows = input_rows[0];
{% for input in inputs %}
        if (input_data[{{ input.index }}] == nullptr) return 1;
        if (input_widths[{{ input.index }}] != {{ input.row_width }}) return 2;
{% if input.index > 0 %}
        if (input_rows[{{ input.index }}] != rows) return 3;
{% endif %}
{% endfor %}
        std::array<const void*, {{ input_count }}> bases{};
{% for input in inputs %}
        bases[{{ input.index }}] = input_data[{{ input.index }}];
{% endfor %}
        return run_bound_inputs(
            bases,
            rows,
            output_path,
            output_offset_bytes,
            async_writeback_bytes,
            requested_threads,
            parallel_mode,
            pin_threads,
            rows_out,
            seconds_out,
            cpu_seconds_out,
            threads_out,
            available_cpus_out);
    } catch (const std::exception& exc) {
        g_last_error = exc.what();
        return 100;
    } catch (...) {
        g_last_error = "unknown cpp_stream exception";
        return 101;
    }
}