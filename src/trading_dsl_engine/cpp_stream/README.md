# cpp_stream backend

See the existing backend documentation in this directory. The current optimization work is documented in:

- `PARALLELISM.md` for safe row/lane scheduling and final-state merging;
- `CEILING_ARCHITECTURE.md` for the generated-code boundary and generic compiler passes;
- `PROJECTION_FUSION_BENCHMARK.md` and `.json` for controlled before/after results.
