#pragma once

// Literal source types live with the other source descriptors in utils.hpp so
// generic tensor/einsum code can use them without depending on an operator header.
#include "stackdsl/utils.hpp"
